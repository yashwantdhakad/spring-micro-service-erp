package com.monash.erp.mfg.repository;

import com.monash.erp.mfg.entity.WorkEffortPartLocation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WorkEffortPartLocationRepository extends JpaRepository<WorkEffortPartLocation, Long> {
}