package com.monash.erp.wms.repository;

import com.monash.erp.wms.entity.WebAnalyticsConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WebAnalyticsConfigRepository extends JpaRepository<WebAnalyticsConfig, Long> {
}