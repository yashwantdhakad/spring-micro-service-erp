package com.monash.erp.wms.repository;

import com.monash.erp.wms.entity.DataResourceRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DataResourceRoleRepository extends JpaRepository<DataResourceRole, Long> {
}