package com.example.erp.wms.ofbiz.repository;

import com.example.erp.wms.ofbiz.domain.MimeTypeHtmlTemplate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MimeTypeHtmlTemplateRepository extends JpaRepository<MimeTypeHtmlTemplate, Long> {
}