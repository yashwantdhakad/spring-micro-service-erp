package com.example.erp.wms.ofbiz.repository;

import com.example.erp.wms.ofbiz.domain.WebAnalyticsType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WebAnalyticsTypeRepository extends JpaRepository<WebAnalyticsType, Long> {
}