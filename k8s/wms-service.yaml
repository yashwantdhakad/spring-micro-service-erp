apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wms-service
  template:
    metadata:
      labels:
        app: wms-service
    spec:
      containers:
        - name: wms-service
          image: spring-micro-service-erp/wms-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: DB_HOST
              value: host.docker.internal
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: wmsdb
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              value: "123456"
---
apiVersion: v1
kind: Service
metadata:
  name: wms-service
spec:
  type: ClusterIP
  selector:
    app: wms-service
  ports:
    - port: 8082
      targetPort: 8082
