apiVersion: apps/v1
kind: Deployment
metadata:
  name: oms-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oms-service
  template:
    metadata:
      labels:
        app: oms-service
    spec:
      containers:
        - name: oms-service
          image: spring-micro-service-erp/oms-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: DB_HOST
              value: host.docker.internal
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: omsdb
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              value: "123456"
---
apiVersion: v1
kind: Service
metadata:
  name: oms-service
spec:
  type: ClusterIP
  selector:
    app: oms-service
  ports:
    - port: 8081
      targetPort: 8081
