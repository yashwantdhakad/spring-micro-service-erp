package com.example.erp.mfg.ofbiz.repository;

import com.example.erp.mfg.ofbiz.domain.TechDataCalendarWeek;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TechDataCalendarWeekRepository extends JpaRepository<TechDataCalendarWeek, Long> {
}