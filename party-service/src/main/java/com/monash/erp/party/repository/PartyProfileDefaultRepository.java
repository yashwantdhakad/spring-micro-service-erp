package com.monash.erp.party.repository;

import com.monash.erp.party.entity.PartyProfileDefault;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartyProfileDefaultRepository extends JpaRepository<PartyProfileDefault, Long> {
}