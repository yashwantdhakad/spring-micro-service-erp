package com.monash.erp.oms.common.repository;

import com.monash.erp.oms.common.entity.CustomScreenType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomScreenTypeRepository extends JpaRepository<CustomScreenType, Long> {
}
