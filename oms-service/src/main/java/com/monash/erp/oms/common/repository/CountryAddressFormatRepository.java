package com.monash.erp.oms.common.repository;

import com.monash.erp.oms.common.entity.CountryAddressFormat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CountryAddressFormatRepository extends JpaRepository<CountryAddressFormat, Long> {
}
