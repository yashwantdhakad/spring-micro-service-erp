package com.example.erp.oms.common.repository;

import com.example.erp.oms.common.domain.CustomScreen;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomScreenRepository extends JpaRepository<CustomScreen, Long> {
}
